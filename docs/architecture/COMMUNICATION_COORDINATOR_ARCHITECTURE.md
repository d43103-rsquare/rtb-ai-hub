> **Status: DESIGN_ONLY** — 4-Layer 아키텍처 설계 문서. 실제 구현은 debate engine + workflow engine 기반으로 다른 형태로 구현됨.

# Communication Coordinator Architecture Design

> 💡 **이 문서는 기술 아키텍처 설계서입니다.**  
> 비개발자 또는 개념 이해가 먼저 필요하신 분은 **[개념 설명서 (CONCEPTS.md)](../CONCEPTS.md)**를 먼저 읽어보세요!

---

## 시스템 아키텍처 개요

### 1.1 설계 원칙 (Design Principles)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Communication Coordinator Architecture                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────────┐ │
│  │                        PRESENTATION LAYER                               │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐ │ │
│  │  │   Slack     │  │  Dashboard  │  │   Jira      │  │   GitHub/PR     │ │ │
│  │  │   Integration│  │   (React)   │  │   Plugin    │  │   Comments      │ │ │
│  │  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └────────┬────────┘ │ │
│  └─────────┼────────────────┼────────────────┼──────────────────┼──────────┘ │
│            │                │                │                  │            │
│  ┌─────────▼────────────────▼────────────────▼──────────────────▼──────────┐ │
│  │                     COMMUNICATION COORDINATOR LAYER                      │ │
│  │  ┌─────────────────────────────────────────────────────────────────────┐ │ │
│  │  │              Communication Pattern Engine                           │ │ │
│  │  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌─────────────┐ │ │ │
│  │  │  │ Requirement  │ │  Design      │ │ Implementation│ │  Testing   │ │ │ │
│  │  │  │Clarification │ │   Review     │ │     Sync      │ │  Handoff   │ │ │ │
│  │  │  └──────────────┘ └──────────────┘ └──────────────┘ └─────────────┘ │ │ │
│  │  └─────────────────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────────────────┐ │ │
│  │  │                 Role Adapter / Translation Layer                    │ │ │
│  │  │     PM ⇄ System Planner ⇄ UX ⇄ UI Dev ⇄ Backend ⇄ QA ⇄ Ops         │ │ │
│  │  └─────────────────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────────────────┐ │ │
│  │  │              Active Wiki Facilitation System                        │ │ │
│  │  │  - Auto-suggest relevant wiki sections                              │ │ │
│  │  │  - Detect knowledge gaps                                            │ │ │
│  │  │  - Create concept relationship maps                                 │ │ │
│  │  └─────────────────────────────────────────────────────────────────────┘ │ │
│  │  ┌─────────────────────────────────────────────────────────────────────┐ │ │
│  │  │              Onboarding Coordination System                         │ │ │
│  │  │  - Structured learning paths per role                               │ │ │
│  │  │  - Mentor matching                                                  │ │ │
│  │  │  - Contextual help based on current work                            │ │ │
│  │  └─────────────────────────────────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│            │                                                                  │
│  ┌─────────▼────────────────────────────────────────────────────────────────┐ │
│  │                      CORE ENGINE LAYER (Existing)                        │ │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │ │
│  │  │ Multi-Agent │  │   Context   │  │    Wiki     │  │  Smart Handoff  │  │ │
│  │  │  Pipeline   │  │   Engine    │  │  Knowledge  │  │     System      │  │ │
│  │  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────────┘  │ │
│  └──────────────────────────────────────────────────────────────────────────┘ │
│            │                                                                   │
│  ┌─────────▼─────────────────────────────────────────────────────────────────┐ │
│  │                      INFRASTRUCTURE LAYER                                  │ │
│  │  PostgreSQL │ Redis │ BullMQ │ MCP Servers │ Slack API │ Wiki Repository │ │
│  └───────────────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────────────────────────────┘
```

#### 핵심 설계 원칙

1. **Event-Driven Communication Flow**
   - 모든 커뮤니케이션은 이벤트로 취급
   - 발행-구독 패턴으로 느슨한 결합 유지
   - 비동기 처리로 응답성 보장

2. **Role-Centric Design**
   - 모든 기능은 역할(Role)을 중심으로 설계
   - 동일한 정보이라도 역할별로 다른 형태로 제공
   - 역할 간 번역이 일급 시민(First-class citizen)

3. **Progressive Enhancement**
   - 기존 인프라를 재사용하며 점진적 확장
   - 새 컴포넌트는 기존 에이전트/워크플로우와 통합
   - 하위 호환성 유지

4. **Human-in-the-Loop**
   - AI는 제안하고 조율, 인간이 최종 결정
   - 모든 자동화는 opt-in 기반
   - 오버라이드 메커니즘 제공

5. **Context Preservation**
   - 대화/작업의 맥락을 모든 단계에서 유지
   - Cross-reference engine과 연동
   - 히스토리 기반 개인화

---

## 1.2 시스템 컴포넌트 구조

```
packages/workflow-engine/src/
├── agents/                          # 기존: 멀티 에이전트 파이프라인
│   ├── base-agent.ts
│   ├── orchestrator.ts
│   ├── implementations/
│   └── ...
│
├── communication/                   # NEW: 통신 코디네이터 컴포넌트
│   ├── coordinator-service.ts       # 메인 서비스 진입점
│   ├── patterns/                    # Communication Pattern Engine
│   │   ├── pattern-registry.ts
│   │   ├── pattern-engine.ts
│   │   ├── types.ts
│   │   └── implementations/         # 각 패턴 구현
│   │       ├── requirement-clarification.ts
│   │       ├── design-review.ts
│   │       ├── implementation-sync.ts
│   │       └── testing-handoff.ts
│   │
│   ├── adapters/                    # Role Adapter Layer
│   │   ├── adapter-registry.ts
│   │   ├── base-adapter.ts
│   │   ├── types.ts
│   │   └── implementations/         # 역할별 어댑터
│   │       ├── pm-adapter.ts
│   │       ├── system-planner-adapter.ts
│   │       ├── ux-planner-adapter.ts
│   │       ├── ui-dev-adapter.ts
│   │       ├── backend-dev-adapter.ts
│   │       ├── qa-adapter.ts
│   │       └── ops-adapter.ts
│   │
│   ├── wiki-facilitator/            # Active Wiki Facilitation
│   │   ├── facilitator-service.ts
│   │   ├── suggestion-engine.ts
│   │   ├── gap-detector.ts
│   │   ├── concept-mapper.ts
│   │   └── types.ts
│   │
│   └── onboarding/                  # Onboarding Coordination
│       ├── onboarding-service.ts
│       ├── path-generator.ts
│       ├── mentor-matcher.ts
│       ├── progress-tracker.ts
│       └── types.ts
│
├── utils/                           # 기존 유틸리티
│   ├── wiki-knowledge.ts
│   ├── context-engine.ts
│   ├── role-notifier.ts
│   └── ...
│
└── workflows/                       # 기존 워크플로우
    └── ...

packages/shared/src/
├── communication-types.ts           # NEW: 통신 코디네이터 공유 타입
└── ...

drizzle/
├── 0005_add_communication_tables.sql # NEW: 커뮤니케이션 테이블 마이그레이션
└── ...
```

---

## 1.3 데이터 흐름 다이어그램

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         Communication Flow Example                            │
│                    PM이 기능 요청 → 개발자가 구현하는 시나리오                   │
└──────────────────────────────────────────────────────────────────────────────┘

[PM] "사용자가 로그인할 수 있게 해주세요"
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  STEP 1: Pattern Detection                                                 │
│  - requirement-clarification 패턴 감지                                      │
│  - 필요한 역할 식별: System Planner, UX Planner, Backend Dev                │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  STEP 2: Role-Specific Translation                                         │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐             │
│  │ System Planner  │  │  UX Planner     │  │  Backend Dev    │             │
│  │ Adapter         │  │  Adapter        │  │  Adapter        │             │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘             │
│           │                    │                    │                       │
│  "인증 방식 선정 필요:       "로그인 UX 흐름:      "API 설계 필요:         │
│   JWT vs Session vs OAuth"   이메일/비밀번호/소셜"  /auth/login POST"      │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  STEP 3: Wiki Facilitation                                                 │
│  - 관련 wiki 문서 자동 추천: auth-flow.md, social-login-guide.md           │
│  - 과거 유사 프로젝트 컨텍스트: PROJ-098 (회원가입) 참조 제안               │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  STEP 4: Multi-Agent Coordination                                          │
│  - 각 역할별 어댑터가 Analyzer→Planner→Developer 파이프라인 활용           │
│  - 병렬 실행 후 결과 통합                                                   │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│  STEP 5: Synthesis & Delivery                                              │
│  - 통합된 명세서 생성                                                       │
│  - PM에게: "다음 사항이 확인 필요합니다..."                                 │
│  - 개발자에게: "구현 명세: [상세 기술 스펙]"                                │
└─────────────────────────────────────────────────────────────────────────────┘
    │
    ▼
[PM] 확인/수정 → 개발자에게 전달 → 개발 시작
```

---

## 1.4 통합 아키텍처

### 기존 시스템과의 통합

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                        Integration Architecture                               │
└──────────────────────────────────────────────────────────────────────────────┘

                    ┌─────────────────────┐
                    │   Webhook Events    │
                    │  (Jira, GitHub, etc)│
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   Event Router      │
                    │  (기존 webhook-listener)
                    └──────────┬──────────┘
                               │
           ┌───────────────────┼───────────────────┐
           │                   │                   │
           ▼                   ▼                   ▼
    ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
    │   Workflow   │   │Communication │   │   Context    │
    │    Engine    │   │  Coordinator │   │    Engine    │
    │  (기존)       │   │   (NEW)      │   │  (기존)       │
    └──────┬───────┘   └──────┬───────┘   └──────┬───────┘
           │                  │                  │
           │         ┌────────▼────────┐         │
           │         │                 │         │
           │    ┌────▼────┐      ┌────▼────┐    │
           │    │ Patterns│      │Adapters │    │
           │    └────┬────┘      └────┬────┘    │
           │         │                │         │
           └─────────┼────────────────┼─────────┘
                     │                │
                     ▼                ▼
              ┌──────────────────────────┐
              │   Multi-Agent Pipeline   │
              │  (Analyzer→Planner→Dev)   │
              └────────────┬─────────────┘
                           │
                           ▼
              ┌──────────────────────────┐
              │    Slack/Notification    │
              └──────────────────────────┘
```

---

## 다음 단계

1. **Communication Pattern Engine** 상세 설계
2. **Role Adapter / Translation Layer** 설계
3. **Active Wiki Facilitation** 설계
4. **Onboarding Coordination** 설계
5. **데이터 모델 및 API** 설계
6. **통합 및 확장 전략** 문서화

각 컴포넌트별로 상세 타입 정의, 알고리즘, 데이터베이스 스키마를 설계하시겠습니까?
