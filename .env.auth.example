# ============================================================================
# AUTHENTICATION CONFIGURATION
# ============================================================================
# 인증 관련 설정 (Google OAuth, JWT, DEV_MODE)
# 
# ⚠️  민감한 정보 포함 - 절대 커밋하지 마세요!
# 복사 방법: cp .env.auth.example .env.auth
# ============================================================================

# ─── Dev Mode (로컬 개발 전용) ──────────────────────────────────────────────
# true로 설정하면 Google OAuth 없이 /auth/dev/login으로 즉시 로그인
DEV_MODE=true
DEV_USER_EMAIL=your-email@company.com
DEV_USER_NAME=Your Name

# ─── Google OAuth (프로덕션용) ──────────────────────────────────────────────
# DEV_MODE=false일 때 필수
# Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성 필요
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:4001/auth/google/callback
ALLOWED_WORKSPACE_DOMAINS=your-company.com

# ─── Security Keys ──────────────────────────────────────────────────────────
# JWT 토큰 서명용 (최소 32자 이상)
# 생성 방법: node scripts/generate-secrets.js
JWT_SECRET=your-jwt-secret-key-here-minimum-32-characters

# 자격증명 암호화용 (64자 hex)
# 생성 방법: node scripts/generate-secrets.js
CREDENTIAL_ENCRYPTION_KEY=your-encryption-key-64-hex-characters
